/**
 * Custom Components Extension
 *
 * Add custom UI component overrides here.
 *
 * Example:
 * ```typescript
 * // Override the default message renderer
 * import { registry } from '@chaaskit/client';
 *
 * const CustomMessageRenderer = ({ message }) => {
 *   // Custom rendering logic
 *   return <div>{message.content}</div>;
 * };
 *
 * registry.register('content-renderer', 'custom-message', CustomMessageRenderer);
 * ```
 */

export {};
