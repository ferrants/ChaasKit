# Database (required)
DATABASE_URL="postgresql://user:password@localhost:5432/chaaskit_demo?schema=public"

# Auth secrets (required - generate with: openssl rand -base64 32)
SESSION_SECRET="replace-me-with-32+chars"
JWT_SECRET="replace-me-with-32+chars"

# AI Provider (at least one required)
OPENAI_API_KEY="sk-..."
# ANTHROPIC_API_KEY="sk-ant-..."

# Application URLs (optional in development)
APP_URL="http://localhost:5173"
API_URL="http://localhost:3000"

# Optional: extra CORS origins (comma-separated)
# CORS_ALLOWED_ORIGINS="https://example.com,https://app.example.com"
